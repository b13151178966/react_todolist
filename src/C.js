import React, { Component } from 'react';

export default class C extends Component{
   render(){
     return (
       <div class="">这是React 的 C页面</div>
      //  <div>{this.props.match.parems.ReactDOM}</div>
     )
   }
   componentWillMount(){
     
   }
} 